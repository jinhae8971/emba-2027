# EMBA 2027 입학 준비 대시보드 — 디자인 전면 개편 명세서

> **문서 목적**: 이 문서 하나만 읽고 UI/UX를 처음부터 다시 설계할 수 있도록, 현행 시스템의 구조·데이터·동작 계약을 전부 기술하고 재설계에 필요한 요구사항을 정의한다.
> **작성 기준일**: 2026-08-17
> **현행 산출물**: `index.html` 단일 파일 (약 1,130줄) · https://jinhae8971.github.io/emba-2027/
> **디자인 자유도**: **전면 자유.** 프레임워크 도입, 라이트 테마, 다중 파일 분리 모두 허용. 단 4장의 *기능·데이터 계약*은 유지되어야 한다.

---

## 1. 제품 정의

### 1.1 한 문장 정의

서울대학교 EMBA와 KAIST Executive MBA의 **2027학년도 봄학기 지원**을 준비하는 **단 한 명의 지원자**가, 원서 마감(2026-10-08)까지 남은 약 7주 동안 매일 열어보며 "오늘 무엇을 해야 하는가"를 확인하고 진행 상황을 기록하는 개인용 도구.

### 1.2 사용자 프로필 (단일 사용자, 페르소나 아님 — 실제 인물)

| 항목 | 값 |
|---|---|
| 직장 경력 | 8년 |
| 학력 | 박사 학위 보유 |
| 지원 경로 | 회사가 위탁교육 협약을 체결하는 **사내 위탁 트랙** |
| 1순위 | 서울대 EMBA (박사학위자 경력요건 감면 조항으로 자격 충족) |
| 2순위 | KAIST Executive MBA (경력 10년 요건, 박사 감면 조항 미확인 — 불가 시 KAIST Professional MBA로 전환) |
| 주 사용 기기 | **모바일(휴대폰)이 일상 사용, PC는 집중 작업 시** |
| 사용 빈도 | 하루 1~3회, 짧게 (모바일) / 주 1~2회, 길게 (PC) |

### 1.3 핵심 사용 시나리오 (JTBD)

1. **[모바일·10초]** 출근길에 열어서 "오늘 마감인 게 뭐지?"를 확인하고 끝난 것을 체크한다.
2. **[모바일·30초]** 회의 중 확정된 일정("인사팀 결재 9/3")을 즉시 등록한다.
3. **[PC·30분]** 자기소개서 6블록을 작성하고 텍스트로 내보내 한글 파일에 붙여넣는다.
4. **[PC·15분]** 회사 지원 비율이 확정되면 비용 시뮬레이터 숫자를 갱신해 실부담액을 재계산한다.
5. **[PC·60분]** 면접 3주 전, 18개 질문 답변 스크립트를 작성하고 내보내 인쇄한다.
6. **[모바일·1분]** 주말에 전체 진척도를 훑고 뒤처진 영역을 파악한다.

> **설계 함의**: 1·2·6번(모바일, 초 단위)이 **일상 사용의 80%**이고, 3·4·5번(PC, 분 단위)이 **가치의 80%**다. 현재 디자인은 이 두 모드를 구분하지 않는다. 이것이 개편의 핵심 과제다.

---

## 2. 현행 시스템 구조

### 2.1 기술 스택 · 배포

| 항목 | 현행 |
|---|---|
| 구현 | 순수 HTML + CSS + Vanilla JS **단일 파일** |
| 문법 | ES5 스타일 (`var`, `function`, 화살표함수·템플릿리터럴 미사용) |
| 외부 의존성 | **0개.** CDN·폰트·아이콘 전부 없음 (인라인 SVG data-URI 파비콘만 사용) |
| 빌드 | 없음. 파일을 그대로 브라우저에서 열면 동작 |
| 배포 | GitHub Pages 정적 호스팅 (`jinhae8971/emba-2027`, public) |
| 저장소 | 브라우저 `localStorage` 전용. 서버·계정·동기화 없음 |
| PWA | `manifest.webmanifest` + `apple-mobile-web-app-capable` (홈화면 추가 시 전체화면) |

### 2.2 파일 구성

```
index.html              대시보드 본체 (단일 파일, 외부 의존성 없음)
manifest.webmanifest    홈 화면 추가용 PWA 매니페스트
icon.svg                앱 아이콘
setup_github.ps1        배포 스크립트 (.gitignore로 커밋 제외)
README.md               사용 설명
DESIGN_SPEC.md          본 문서
```

### 2.3 정보 구조 — 6개 탭

| # | `section` id | 탭 레이블 (PC / 모바일) | 내용 |
|---|---|---|---|
| 1 | `ov` | 개요 & 리스크 / 개요 | 오늘 할 일, 리스크 알림 4종, Top5, 진척도, 통합 타임라인, 내 일정 등록 |
| 2 | `cmp` | 학교 비교 / 비교 | 3개 과정 비교표, KAIST 재학생 경력분포·업종구성, 커리큘럼 |
| 3 | `chk` | 일정 & 서류 체크리스트 / 체크 | 내 할 일 등록, 미완료 필터, 서울대/KAIST/공통 체크리스트, 전형일정표, 제출서류 6종, 추천인 트래커 |
| 4 | `essay` | 자기소개서 관리 / 자소서 | 스토리라인 6블록 입력, 퇴고 체크리스트, 텍스트 내보내기 |
| 5 | `cost` | 비용 시뮬레이터 / 비용 | 11개 입력값 → 3개 과정별 실부담 계산, 비교 막대, 재무 리스크 |
| 6 | `itv` | 면접 Q&A 뱅크 / 면접 | 18문항(5개 카테고리) + 힌트 + 답변 작성 + 내보내기, 준비물 체크, 피해야 할 답변 |

탭 전환은 `nav button[data-t]` 클릭 → 모든 `section`에서 `.on` 제거 후 대상에 부여 → `window.scrollTo(top)`.

### 2.4 데이터 모델 — **변경 금지 계약**

#### (a) 체크리스트 `LISTS` (총 60항목)

```js
var LISTS = {
  top5:   [ /* 5개  */ ],   // 최우선 액션
  snu:    [ /* 15개 */ ],   // 서울대 준비
  kaist:  [ /* 12개 */ ],   // KAIST 준비
  common: [ /* 10개 */ ],   // 공통 서류
  essay:  [ /* 10개 */ ],   // 자기소개서 퇴고
  itv:    [ /*  8개 */ ]    // 면접 준비물
};
// 항목 스키마
{ t: '항목 제목(필수)', due: 'YYYY-MM-DD(선택)', d: '보조 설명(선택)' }

var SRC = { top5:'최우선', snu:'서울대', kaist:'KAIST',
            common:'공통 서류', essay:'자기소개서', itv:'면접' };  // 출처 배지 레이블
```

#### (b) 일정 `EVENTS` (13건, 고정) · `DDAY` (6건, 고정)

```js
{ d:'2026-10-08', t:'서울대 마감 18:00 / KAIST 마감 17:30 (동일일)', s:'snu' }
// s = 'snu' | 'kaist'  → 학교별 색상 구분에 사용
```

주요 확정 일정: KAIST 설명회 8/19·8/22 · 서울대 접수시작 9/7 · KAIST 접수시작 9/29 · **양교 동시 마감 10/8** · TEPS 특별시험 10/14 · 1단계 발표 10/30 · **면접 11/4(수, 연차 필요)** · 최종발표 11/23 · 등록 11/25~12/9 · 충원 12/14·12/21·12/28.

#### (c) 영속 상태 `localStorage`

```js
key: 'emba2027_v1'
{
  chk:  { top5:[bool×5], snu:[bool×15], kaist:[bool×12],
          common:[bool×10], essay:[bool×10], itv:[bool×8] },
  ev:   [ {d:'YYYY-MM-DD', t:'내용'} ],              // 사용자 등록 일정
  task: [ {d:'YYYY-MM-DD'|'', t:'내용', done:bool} ],// 사용자 등록 할 일
  rec:  [ {r:'구분', n:'성함', d:'MM/DD', s:'미요청|요청함|수령완료'} ],
  f:    { 'e_pos':'...', 'c_snu':'60000000', 'q0':'...' }  // id 접두사 e_ / c_ / q숫자 인 모든 입력
}
```

> `f`는 `document` 레벨 `input` 이벤트에서 `/^(e_|c_|q\d)/` 정규식으로 id를 매칭해 자동 저장한다. **입력 필드의 id 접두사 규칙을 바꾸면 기존 사용자 데이터가 유실된다.**

### 2.5 핵심 함수 계약

| 함수 | 역할 |
|---|---|
| `diff(dateStr)` | 오늘 기준 남은 일수 (음수 = 지남) |
| `dueTag(dateStr)` | D-Day 배지 HTML 반환. 임계값 `≤3일 위험 / ≤10일 주의 / ≤30일 예고 / 그 외 보통` |
| `fit(key)` | 항목 수 증감 시 기존 체크를 보존하며 배열 길이만 조정 (**비파괴**) |
| `render(key, mountId, barId, pctId)` | 리스트 렌더 + 진행바 갱신. `ONLY=true`면 완료 항목 숨김 |
| `drawFocus()` | 전 리스트에서 *미완료 + 마감 14일 이내* 항목 + 미완료 사용자 할 일을 마감 순 정렬, 최대 12개 |
| `drawDday()` | `DDAY` + 사용자 일정 병합 → 종료 3일 초과분 제외 → 최대 6개 |
| `drawTL()` | `EVENTS` + 사용자 일정 병합, 날짜 순 전체 표시 |
| `renderProg()` | 영역별 막대 + 헤더 전체 진척도(`전체 체크 60개 + 사용자 할 일` 합산) |
| `calc()` | 비용 재계산. 11개 입력 → 3개 과정 카드 + 비교 막대 |
| `addEv/delEv/addTask/delTask/toggleTask` | 사용자 데이터 CRUD, 즉시 `save()` |
| `exportEssay()/exportItv()` | 텍스트 조립 후 textarea에 출력 + `select()` |

이벤트 위임 구조:

```
document 'change' → checkbox 판별
   ├ [data-k][data-i] → LISTS 체크 토글 → renderAll() + save()
   ├ [data-task]      → 사용자 할 일 토글 → 부분 렌더 + save()
   └ #onlyopen        → ONLY 필터 토글 → renderAll()
document 'input'  → id가 /^(e_|c_|q\d)/ → DB.f 저장
```

### 2.6 필수 DOM 마운트 포인트

디자인을 어떻게 바꾸든 **아래 id를 가진 요소가 존재하고 같은 의미로 쓰여야** 스크립트가 동작한다. (구조·태그·클래스는 전부 자유)

```
표시 전용 : #today #ddays #tl #focus #mytasks #prog #costOut #costBars #qbank #recTb #toast
            #allbar #allpct #top5 #top5bar #top5pct
리스트    : #ck-snu #ck-kaist #ck-common #ck-essay #ck-itv
            (+ #bar-* / #pct-* 각각)
입력      : #ev_d #ev_t #mt_d #mt_t #onlyopen #swwrap
자소서    : #e_pos #e_why #e_car #e_gap #e_plan #e_fit #eout #eouttx
비용      : #c_snu #c_kai #c_pmba #c_sup #c_ent #c_fee #c_trip #c_book #c_life #c_mon #c_bond
면접      : #q0 ~ #q17 #iout #iouttx
탭        : nav button[data-t] ↔ section#ov #cmp #chk #essay #cost #itv
```

### 2.7 현행 디자인 토큰 (참고용 — 교체 가능)

```css
--bg:#0d1117  --panel:#161b22  --panel2:#1c2230  --line:#2a3140
--tx:#e6edf3  --tx2:#9aa7b8    --tx3:#6e7d90
--snu:#3b82f6 --kaist:#f59e0b                    /* 학교 식별색 */
--ok:#22c55e  --warn:#f97316   --bad:#ef4444     /* 상태색 */
--acc:#7c3aed                                    /* 브랜드 액센트 */
```

본문 14px / 행간 1.6 / `Pretendard` 우선 한글 스택 / 최대 폭 1180px / 카드 radius 12px.
모바일(≤640px)에서는 상단 탭이 **하단 고정 6칸 탭바**로 전환된다.

---

## 3. 현행 UX 진단 — 개편이 필요한 이유

| # | 문제 | 근거 | 심각도 |
|---|---|---|---|
| P1 | **모드 미분화** — 모바일 10초 확인용과 PC 30분 작성용이 같은 레이아웃 | 시나리오 1·2가 사용 빈도의 80%인데 진입 화면이 정보 나열형 | 높음 |
| P2 | **정보 밀도 과다** — 개요 탭에 카드 7개, 스크롤 3~4화면 | "오늘 할 일"이 최상단이지만 그 아래 리스크·Top5·진척도·타임라인이 이어짐 | 높음 |
| P3 | **시각적 우선순위 부재** — 마감 D-3 항목과 D-45 항목이 배지 색상 외에 동일 무게 | `dueTag` 4단계 색상만으로 긴급도를 표현 | 높음 |
| P4 | **입력 흐름 단절** — 일정 등록은 개요 탭, 할 일 등록은 체크리스트 탭으로 분리 | 사용자 관점에서는 둘 다 "생각난 걸 적는다"는 동일 행위 | 중 |
| P5 | **진척도 지표 3중복** — 헤더 전체바 / 영역별 막대 / 리스트별 막대가 같은 데이터를 세 번 표현 | `#allbar`, `#prog`, `#bar-*` | 중 |
| P6 | **비용·면접 탭의 낮은 접근성** — 6개 탭이 동등한 위계지만 실사용 빈도는 극단적으로 다름 | 면접 탭은 11월에만, 비용 탭은 총 2~3회 사용 | 중 |
| P7 | **완료의 보상 부재** — 체크해도 취소선만 생기고 진행감·성취감 피드백이 약함 | 7주간 매일 여는 도구에서 지속 사용 동기가 약함 | 중 |
| P8 | **데이터 신뢰도 표기 미흡** — "요강 확인 필요"인 미확정 정보와 확정 정보가 같은 시각 언어 | 등록금·KAIST 면접일 등은 추정치 | 낮음 |
| P9 | **데이터 휘발 위험 경고 부족** — localStorage 전용인데 백업 유도가 약함 | 브라우저 데이터 삭제 시 전량 소실 | 낮음 |

---

## 4. 재설계 요구사항

### 4.1 반드시 유지 (기능·데이터 계약)

- **F1.** 2.4의 데이터 스키마와 `localStorage` 키(`emba2027_v1`) 및 필드 구조 — 기존 저장 데이터의 하위 호환.
- **F2.** 2.6의 DOM id 계약 — 또는 스크립트를 함께 수정하되 **모든 기능이 동등하게 동작**할 것.
- **F3.** 6개 탭의 모든 기능 (기능 삭제 금지, 재배치·통합은 허용).
- **F4.** 60개 체크 항목·13개 일정·18개 면접 문항의 **내용 텍스트 원문** (문구 손실 금지).
- **F5.** 사용자 직접 등록(일정·할 일)의 추가·완료·삭제 전체 경로.
- **F6.** 자기소개서·면접 답변의 텍스트 내보내기(복사 가능 형태).
- **F7.** 서버 전송 없음 — 모든 데이터가 기기 안에 머무를 것.
- **F8.** 오프라인 동작 (네트워크 없이 파일만으로 실행).

### 4.2 완전 자유

프레임워크(React/Svelte/Vue 등) 도입, 다중 파일 구성 및 빌드 파이프라인, 라이트 테마 또는 테마 토글, 타이포그래피·컬러 시스템 전면 교체, 아이콘 세트 도입, 레이아웃 패러다임 변경(탭 → 사이드바/스택/카드덱 등), 애니메이션·마이크로인터랙션 추가, 정보 구조 재편(탭 통폐합 포함).

> 단, 단일 HTML 파일 구조를 벗어나면 GitHub Pages 배포 절차가 바뀐다. 프레임워크를 도입할 경우 **빌드 산출물을 `docs/` 또는 `gh-pages`로 배포하는 방식**을 함께 제안해 달라.

### 4.3 개편 목표 (성공 기준)

| 목표 | 측정 가능한 기준 |
|---|---|
| G1. 10초 확인 완료 | 모바일에서 앱을 열고 **스크롤 없이** 오늘/이번 주 마감 항목과 다음 D-Day를 파악할 수 있다 |
| G2. 3탭 이내 입력 | 생각난 일정·할 일을 **3번의 탭 조작 이내**로 등록 완료 |
| G3. 긴급도 즉시 판독 | 색상 없이 흑백으로 봐도 마감 임박 항목이 구분된다 (크기·위치·형태로도 위계 표현) |
| G4. 사용 빈도 반영 | 자주 쓰는 기능이 상위 계층, 드물게 쓰는 기능(비용·비교)이 하위 계층 |
| G5. 진행감 제공 | 체크 시 명확한 시각 피드백, 남은 일 수가 줄어드는 것이 보인다 |
| G6. 신뢰도 구분 | 확정 정보와 "요강 확인 필요" 정보가 시각적으로 다르다 |

### 4.4 화면별 요구사항

**① 홈 / 개요**
- 최우선 노출: **오늘·이번 주 마감 항목** + **다음 결정적 D-Day 1~2개**.
- 리스크 알림 4종(전략 판정·협약 시점·KAIST 감면 미확인·미확정 정보)은 접근 가능하되 **매일 보는 시야를 막지 않을 것**. (현행: `<details>` 접이식)
- 통합 타임라인은 전체 조망용 — 홈에서 분리하거나 접이식/별도 뷰로 이동해도 무방.
- 진척도 표현은 **하나의 일관된 체계**로 통합 권장 (현행 3중복 해소).

**② 체크리스트**
- 60항목 + 사용자 할 일을 **마감 기준으로도, 영역 기준으로도** 볼 수 있으면 이상적.
- "미완료만 보기" 필터 유지. 필터 상태의 영속화 여부는 설계 판단에 위임.
- 각 항목: 제목 / 마감 배지 / 보조 설명(2줄 내외) / 출처 영역 표시.
- 추천인 트래커(3~n행, 4열: 구분·성함·요청일·상태)는 표 형태 유지 또는 카드형 전환 자유.

**③ 자기소개서**
- 6블록(포지셔닝 / Why MBA·Why Now / 경력 하이라이트 / 박사×실무 8년 차별화 / 졸업 후 계획 / 학교 선택 이유) 순차 작성 흐름.
- 각 블록의 placeholder는 작성 가이드 역할 — **문구 유지 필수**.
- 긴 텍스트 입력이 주 작업이므로 **집중 모드(넓은 입력 영역, 주변 요소 최소화)** 를 고려할 것.
- 퇴고 체크리스트 10항목은 작성 중 참조 가능한 위치에.

**④ 비용 시뮬레이터**
- 입력 11개 → 결과 3카드 + 비교 막대. 입력 변경 시 **즉시 재계산**(현행 `oninput`).
- 모바일에서 숫자 입력 11개는 부담이 크다 — 단계형·아코디언·프리셋 등 개선 여지 큼.
- 강조 지표: **본인 실부담 총액**, 월 평균 부담, **중도 퇴사 시 반환 위험액**.

**⑤ 면접 Q&A**
- 18문항 / 5카테고리(기본·압박·경력·전략·계획). 문항별 힌트 + 답변 textarea.
- 시험 3주 전 집중 사용 — **연습 모드**(한 문항씩 넘기기, 시간 측정 등)는 선택적 제안 환영.

**⑥ 학교 비교**
- 3개 과정 비교표 + KAIST 재학생 경력분포·업종구성 + 커리큘럼. 참조 빈도가 가장 낮음.
- 표의 가로 스크롤(현행 `.tblwrap`)이 모바일에서 불편 — 카드형 재구성 등 개선 대상.

### 4.5 상태 · 엣지 케이스 (모두 디자인 필요)

| 상황 | 필요 화면 |
|---|---|
| 첫 방문 (저장 데이터 0) | 사용자 할 일 비어 있음, 체크 0/60 |
| 마감 2주 내 항목이 전부 완료됨 | 오늘 할 일 카드의 완료 상태 |
| 마감일이 지난 미완료 항목 | 현행 "N일 지남" 배지 — 더 강한 경고 필요 |
| 사용자 일정이 20건 이상 누적 | 타임라인 과밀 대응 |
| 마감일 없는 사용자 할 일 | 정렬 시 맨 뒤 배치 |
| 저장 실패 (시크릿 모드·용량 초과) | 현행: 토스트 "저장 실패 (브라우저 설정 확인)" |
| 전체 기록 초기화 | 확인 다이얼로그 → 리로드 |
| 지원 시즌 종료 후 (2027-01 이후) | 모든 D-Day가 음수 — 현재 미대응 |

### 4.6 품질 기준

- **접근성**: 본문 대비 WCAG AA(4.5:1) 이상. 색상 단독으로 정보를 전달하지 않을 것(G3). 터치 타깃 최소 44×44px. 체크박스·폼 라벨 연결.
- **모바일 입력**: 입력 필드 폰트 **16px 이상**(iOS 자동 확대 방지). `env(safe-area-inset-bottom)` 대응.
- **성능**: 첫 화면 표시 1초 이내(현행 단일 파일 ~70KB). 애니메이션은 `transform`/`opacity` 중심.
- **한글 조판**: 긴 한글 문장의 줄바꿈·자간·행간 최적화. 숫자는 `tabular-nums`.
- **인쇄**: 자기소개서·면접 스크립트의 인쇄/PDF 출력 스타일 제공 시 가점.

---

## 5. 인수 기준 체크리스트

- [ ] 기존 `emba2027_v1` 저장 데이터가 그대로 복원된다
- [ ] 60개 체크 항목이 모두 존재하고 체크 상태가 유지·저장된다
- [ ] 사용자 일정·할 일의 추가/완료/삭제가 동작하고 새로고침 후에도 남는다
- [ ] 사용자 일정이 타임라인·D-Day·오늘 할 일 세 곳에 모두 반영된다
- [ ] 비용 입력 변경 시 3개 과정 결과가 즉시 재계산된다
- [ ] 자기소개서·면접 답변 내보내기 텍스트가 정상 생성된다
- [ ] 6개 탭(또는 그에 상응하는 구조)의 모든 기능이 동등하게 접근 가능하다
- [ ] 모바일 세로 화면에서 스크롤 없이 오늘 마감 항목이 보인다
- [ ] 네트워크를 끊어도 정상 동작한다
- [ ] 흑백 출력에서도 긴급도 위계가 판별된다

---

## 6. 참고 · 주의사항

- **데이터 정확성**: 등록금(서울대 6,000만 / KAIST EMBA 8,000만 / PMBA 6,000만)과 KAIST 면접일은 **추정치**다. 화면에서 확정 정보와 구분해 표기해야 한다.
- **원본 출처**: 서울대 경영전문대학원 https://gsb.snu.ac.kr · KAIST 경영대학 https://business.kaist.ac.kr
- **미해결 확인 사항**: ① 서울대 박사학위자 경력요건 3년 감면 조항의 2027 요강 원문 확인 ② KAIST EMBA 박사 감면 조항 유무.
- **파일 취급 주의**: 현행 `index.html`은 개행이 많은 단일 파일이다. 부분 편집 시 문자열 리터럴이 잘리지 않도록 전체 파일 기준으로 검증할 것.
